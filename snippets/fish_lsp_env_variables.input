fish_lsp_json_config

the json config to read on startup

fish_lsp_enabled_handlers

enables the fish-lsp handlers 'asciiArt', 'formatting', 'logging', 'snippets', 'completion', 'hover', 'rename', 'definition', 'references', 'diagnostics', 'signatureHelp', 'codeAction', 'index'

fish_lsp_disabled_handlers

disables the fish-lsp handlers 'asciiArt', 'formatting', 'logging', 'snippets', 'completion', 'hover', 'rename', 'definition', 'references', 'diagnostics', 'signatureHelp', 'codeAction', 'index'

fish_lsp_ascii_art  

output help command with ascii art

fish_lsp_diagnostics_comments

include comments in fish-lsp documentation

fish_lsp_enabled_code_actions 

enable the codeActions for fish-lsp (default: "all")

fish_lsp_commit_characters

array of the completion expansion characters. Single letter values only. Commit characters are used to select completion items, as shortcuts. (default: [])

fish_lsp_logfile

path to the logs.txt file (default: '~/path/to/fish-lsp/logs.txt')

fish_lsp_tabSize

amount of spaces in a tab character for the formatter provider (default: 4)

fish_lsp_format_switch_case

keep case statements left aligned with switch block. (default: false)

fish_lsp_default_paths

fish file paths to include as workspaces (default: ['/usr/share/fish', '$HOME/.config/fish'])

fish_lsp_rename_paths

fish file paths that can be renamed by the user (default: ['$HOME/.config/fish'])

fish_lsp_diagnostic_disable_error_codes

disable diagnostics for matching error codes (default: [])